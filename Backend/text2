<div className="relative mb-16">
          <div className="bg-gradient-to-br from-purple-600 via-blue-600 to-indigo-700 rounded-3xl p-8 md:p-12 text-white relative overflow-hidden shadow-2xl">
            <div className="absolute inset-0 opacity-20 z-0">
              <div className="absolute inset-0 bg-gradient-to-r from-purple-800/30 to-blue-800/30"></div>
              <img
                src={heroImage}
                alt="Groundwater analysis background"
                className="w-full h-full object-cover"
              />
            </div>
            <div className="relative z-10">
              <div className="flex flex-col md:flex-row items-center justify-between">
                <div className="flex-1 mb-8 md:mb-0">
                  <h1 className="text-3xl md:text-5xl font-bold mb-4 drop-shadow-xl animate-fade-in">
                    Welcome back, Researcher! 🧪
                  </h1>
                  <p className="text-xl md:text-2xl text-white/90 mb-6">
                    Ready to analyze groundwater quality around you?
                  </p>
                  {/* Stats */}
                  <div className="flex flex-wrap gap-4 mb-6 animate-fade-in" style={{ animationDelay: "150ms" }}>
                    {stats.map(({ label, value, icon: Icon }, idx) => (
                      <div
                        key={label}
                        className={`bg-white/10 backdrop-blur-sm rounded-lg px-4 py-2 flex items-center gap-2 hover:scale-105 transition duration-200 hover:bg-white/20`}
                        tabIndex={0}
                        aria-label={label}
                      >
                        <Icon className={`w-5 h-5 ${stats[idx].color}`} />
                        <div>
                          <div className="font-bold text-lg">{value}</div>
                          <div className="text-sm text-white/80">{label}</div>
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
                <div className="flex-shrink-0 animate-bounce">
                  <div className="w-24 h-24 bg-white/20 backdrop-blur-sm rounded-2xl flex items-center justify-center">
                    <Droplets className="w-12 h-12 text-white" />
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>